# ================================================================
# COMPLETÁ SOLO ESTO — el resto del proyecto ya está conectado
# ================================================================

# 1) Mongo Atlas (gratis): creá un cluster en https://cloud.mongodb.com,
#    "Connect" -> "Drivers" -> copiá la connection string y pegala acá.
#    OJO: Mongo Atlas ya viene como replica set (obligatorio para que
#    Prisma pueda usar $transaction, que usamos en el onboarding).
NEXTAUTH_SECRET ="openssl rand -base64 32"

MONGODB_USERNAME="manuacostaok_db_user"
MONGODB_PASSWORD="4HRWKRxMpUKyoW74"
DATABASE_URL="mongodb+srv://manuacostaok_db_user:4HRWKRxMpUKyoW74@cluster0.rkhsocf.mongodb.net"

# 2) Cualquier string random sirve (para firmar las sesiones).
#    Generalo con: openssl rand -base64 32
NEXTAUTH_SECRET="reemplazame-por-un-string-random"

# 3) Credenciales de Mercado Pago (modo TEST al principio):
#    https://www.mercadopago.com.ar/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN="TEST-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
MERCADOPAGO_PUBLIC_KEY="TEST-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# 4) WhatsApp Cloud API (Meta for Developers) — token y phone number id
#    de tu app de WhatsApp Business:
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""

# ================================================================
# YA CONFIGURADO — no hace falta que toques nada de acá para
# correr en local con `npm run dev`
# ================================================================
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_DOMAIN="haycancha.app"
NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY="${MERCADOPAGO_PUBLIC_KEY}"

# Opcionales, solo si más adelante querés fotos de canchas/torneos
# o el envío de emails transaccionales:
CLOUDINARY_URL=""
RESEND_API_KEY=""
